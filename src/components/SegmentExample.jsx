export default function SegmentExample({ focusStatus }) {
    return (
        <>
            <div style={{margin: "20px auto", border: "2px solid #F5F5F5", width: "90%", height: "35%", minHeight: "100px"}}>
                {/* {focusStatus.active && <p>{focusStatus.field}</p>} */}
            </div>
        </>
    );
}