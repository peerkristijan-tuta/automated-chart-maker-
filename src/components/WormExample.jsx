export default function WormExample({ focusStatus }) {
    return (
        <>
            <div style={{margin: "20px auto", border: "2px solid #F5F5F5", width: "90%", height: "35%"}}>
                {/* {focusStatus.active && <p>{focusStatus.field}</p>} */}
            </div>
        </>
    );
}